namespace TimeTrackerMt.DataRepository.Constants
{
    //Note if ypu add any new value here add same in TypeMaster table in database
    internal enum StatusType
    {
        Site = 1,
        User = 2,
        Delete = 3,
        Active = 4,
        DeActive = 5
    }
}